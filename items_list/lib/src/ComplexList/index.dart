import 'package:flutter/material.dart';

class ComplexList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Complex List"),
    );
  }
}
